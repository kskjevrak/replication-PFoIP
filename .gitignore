# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
.env
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Jupyter
.ipynb_checkpoints
*/.ipynb_checkpoints/*
profile_default/
ipython_config.py

# Data files - exclude large data files but keep synthetic data structure
src/data/raw/
src/data/preprocessing/
!/src/data/raw/.gitkeep
!/src/data/processed/.gitkeep
*.xlsx
*.npy
*.npz
# Note: .parquet files in src/data/ are excluded by default (can be large)
# YAML config files are kept (needed for replication)
# Model files (.pt, .db, .joblib) in results/ are excluded below

# ML specific
#models/*.h5
#models/*.pt
#models/*.pth
#models/*.onnx
#*.npz
#*.npy

# Plots (exclude generated plots globally)
*.png
# Note: specific plot exclusions are handled in Results section below

# Results and artifacts
# Keep directory structure but exclude large generated files
results/tmp_forecasts/
results/models_archive/

# Exclude large forecast and model files (users will generate their own)
results/forecasts/**/*.parquet
results/forecasts/**/*.csv
results/models/**/*.pt
results/models/**/*.pth
results/models/**/*.joblib
results/models/**/*.db

# Keep YAML config files in results (important for replication)
!results/models/**/*.yaml
!results/models/**/*.yml

# Logs - exclude but keep directory structure
results/logs/*.log
!results/logs/.gitkeep

# Plots - exclude generated plots
results/plots/*.png
results/plots/*.pdf
!results/plots/.gitkeep

# tex files
*.tex

# Logs
#logs/
#*.log

# IDE specific files
.idea/
.vscode/
*.swp
*.swo
.spyderproject
.spyproject
.ropeproject

# OS specific
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Cluster specific
slurm-*.out
*-checkpoint
*.out

# Virtual environment
venv/
.venv

# Requirements - keep requirements.txt for pip users
# requirements.txt

# noteboks 
notebooks/
forecast_tables.tex
